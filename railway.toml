[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn surveyor.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
